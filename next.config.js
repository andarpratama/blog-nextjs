/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "miro.medium.com",
      "images.unsplash.com"
    ]
  }
}

module.exports = nextConfig
